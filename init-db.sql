-- Initialize database for Todo App
-- This script ensures the database and user are properly created

-- The database and user are created by PostgreSQL environment variables,
-- but this script can be used for additional initialization if needed.

-- Verify the database exists
SELECT 'Database initialized successfully' as status;
